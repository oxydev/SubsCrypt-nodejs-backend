version: '3.8'
services:
  web:
    network_mode: host
    build:
      context: ./
      target: dev
    volumes:
      - .:/usr/src/app
    command: npm run start
    environment:
      NODE_ENV: development
      DEBUG: nodejs-docker-express:*